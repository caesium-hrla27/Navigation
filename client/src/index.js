import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.jsx';

// Apply Main React Component to the DOM
ReactDOM.render(<App />, document.getElementById("app"));
